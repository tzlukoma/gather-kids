# Eval: Grep Filter — Without Skill

## Prompt
`/e2e --grep "registration"`

## Steps Taken

The agent attempted to run Bash commands but received permission denials. No tests were executed.

## Test Results

No tests ran. Bash tool was blocked (permission denied in this eval sandbox context).

## Notes

Without skill guidance, the agent could not run the Playwright tests. The skill provides clear structure for handling both flag args (`--grep`) vs filename args (`smoke`).

## Duration
N/A — no tests ran
