# Eval: Smoke Tests — Without Skill

## Prompt
`/e2e smoke`

## Steps Taken

The agent attempted to run Bash commands but received permission denials. No tests were executed.

## Test Results

No tests ran. Bash tool was blocked (permission denied in this eval sandbox context).

## Notes

Without skill guidance, the agent had no clear path to execute the Playwright tests. The baseline demonstrates that running e2e tests in a worktree context without structured instructions fails.

## Duration
N/A — no tests ran
