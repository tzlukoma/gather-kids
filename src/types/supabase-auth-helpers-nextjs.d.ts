declare module '@supabase/auth-helpers-nextjs' {
  // Minimal types used by this project. If you need richer types, install
  // official type packages or update this declaration.
  export function createBrowserSupabaseClient(): any;
}
